import 'package:flutter/material.dart';
class StyleTxt extends StatelessWidget{
  StyleTxt({super.key});
  @override
  Widget build(Context){
    return const Text("Hello Khan",style: TextStyle(color: Colors.white,fontSize: 40,);
  }
}